# CAN_ID of the motor
uint8 CAN_ID

# モータに関する設定の種類
# 1 -> enter_control_mode
# 2 -> exit_control_mode
# 3 -> set_position_to_zero
uint8 configration_mode

# 最後のモータかどうか？(enter_control_modeで使用)
# そうであればモータの個数を確定し，メモリ確保などを行う
bool isFinalMotor
---
# Initial state of the motor
AKROS_bridge_msgs/motor_reply_single reply